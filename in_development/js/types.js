// @ts-check

/**
 * @typedef {{ players: string[], elos: number[][], first_season: number[], last_season: number[] }} RawEloData
 * @typedef {{ players: string[], p1s: number[], p2s: number[], seasons: number[], groups: string[] }} RawIgloData
 * @typedef {{ color_on, color_off, line_color_on, line_color_off }} PlayerColors
 */
