// @ts-check

/**
 * @typedef {{ players: string[], elos: number[][], first_season: number[], last_season: number[] }} RawEloData
 * @typedef {{ players: string[], p1s: number[], p2s: number[], seasons: number[], groups: string[] }} RawIgloData
 * @typedef {{ selected_path_color, path_color }} PlayerColors
 * @typedef {{ label: SVGTextElement, path: SVGPathElement, color, selected_color, hidden_color, selected_label_color, selected: boolean }} Series
 */
